{
  "name": "ai-translate-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "author": "Thiago Peres",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thiagoperes/ai-translate.git"
  },
  "bugs": {
    "url": "https://github.com/thiagoperes/ai-translate/issues"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "prepare": "husky",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "lint": "node scripts/check-toolchain.mjs && node scripts/check-workspace-wiring.mjs && pnpm -r --filter './packages/*' lint && oxlint -c .oxlintrc.json --type-aware --deny-warnings bench scripts vitest.base.ts",
    "test": "pnpm -r --filter './packages/*' test",
    "coverage": "pnpm -r --parallel --filter './packages/*' coverage",
    "coverage:strict": "pnpm -r --parallel --filter './packages/*' coverage:strict",
    "test:pack": "pnpm -r --filter './packages/*' test:pack && node ./scripts/package-smoke.mjs",
    "bench": "pnpm run build && node --expose-gc ./bench/state.bench.mjs --scale 0.25 && node --expose-gc ./bench/transaction.bench.mjs --scale 0.25 && node ./bench/prompt.bench.mjs",
    "bench:baseline": "pnpm run build && node --expose-gc ./bench/state.bench.mjs --scale 0.25 --json > ./bench/baseline.json",
    "bench:check": "pnpm run build && node --expose-gc ./bench/state.bench.mjs --scale 0.25 --baseline ./bench/baseline.json",
    "validate": "pnpm run lint && pnpm run typecheck && pnpm run build && pnpm run test && pnpm run test:pack",
    "release:version": "changeset version",
    "release:publish": "changeset publish"
  },
  "devDependencies": {
    "@ai-translate/cli": "workspace:*",
    "@ai-translate/core": "workspace:*",
    "@ai-translate/fs-json": "workspace:*",
    "@ai-translate/markdoc": "workspace:*",
    "@ai-translate/provider-openai": "workspace:*",
    "@arethetypeswrong/cli": "0.18.5",
    "@changesets/changelog-github": "1.0.0",
    "@changesets/cli": "3.0.0",
    "@types/node": "26.2.0",
    "@vitest/coverage-v8": "4.1.10",
    "execa": "10.0.1",
    "husky": "9.1.7",
    "oxlint": "^1.78.0",
    "oxlint-tsgolint": "^7.0.2001",
    "publint": "0.3.23",
    "tsdown": "0.22.14",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "packageManager": "pnpm@10.32.1"
}
